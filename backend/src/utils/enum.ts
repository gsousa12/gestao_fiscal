export enum UserRoleEnum {
  ADMIN = 0,
  EMPLOYEE = 1,
}

export enum CompanyType {
  LEGAL_ENTITY = 0,
  NATURAL_PERSON = 1,
}

export enum TaxRegime {
  SIMPLES_NACIONAL = 0,
  LUCRO_PRESUMIDO = 1,
  LUCRO_REAL = 2,
  LUCRO_ARBITRADO = 3,
  MEI = 4,
}
